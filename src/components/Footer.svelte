<footer>
    <ul>
        <li><a href="/about" alt="about">About</a></li>
    </ul>
</footer>

<style>

    footer {
        height: var(--footer-height);
        background-color:var(--color-dark);
        color: var(--color-white);
        padding: var(--unit) 0;
        width: calc(var(--max-width) + (2 * var(--unit)));
        /* width: 632px; */
        position: relative;
        left: calc(var(--unit) * -1);
        margin: 0 auto;
    }

    ul {
        list-style-type: none;
    }

    a {
        color: var(--color-white);
    }
</style>